{
  "name": "@decky/elasticsearch-lib",
  "version": "1.0.0",
  "description": "Shared Elasticsearch library for Decky platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/mappings dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.11.0",
    "firebase-admin": "^12.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}